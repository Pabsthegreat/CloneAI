# Legacy Tools (`agent/tools/`)

**⚠️ DEPRECATION NOTICE**: This directory is being refactored. Tools are being moved to `agent/skills/`.

## Current Status

*   `mail.py` -> Moved to `agent/skills/mail/`
*   `calendar.py` -> Pending move.
*   `web_search.py` -> Pending move.
*   `documents.py` -> Pending move.

## Purpose

Contains the implementation of various tools used by the agent.
